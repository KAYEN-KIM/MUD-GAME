# 🔧 "두 브랜치가 동일합니다" 문제 해결하기

> **"main and chore/release-candidate-v1 are identical"** 메시지가 나왔어요?  
> 걱정 마세요! 이건 정상이에요. 해결 방법을 알려드릴게요! 😊

---

## 🤔 왜 이런 일이 생겼나요?

**이유:**
- 처음에 모든 코드를 `main`에 올렸어요
- `chore/release-candidate-v1` 브랜치를 만들었지만
- 추가로 변경한 게 없어서 두 브랜치가 똑같아요!

**결론:** 괜찮아요! 그냥 `main` 브랜치를 사용하면 됩니다! 😊

---

## ✅ 해결 방법 (2가지)

### 방법 1: 그냥 main 브랜치 사용하기 (가장 쉬움!)

**이미 모든 코드가 main에 있으니, 그냥 main에서 태그를 만들면 됩니다!**

#### 1단계: PowerShell 열기

Windows 키 → "PowerShell" 입력 → Enter

#### 2단계: 명령어 입력

```powershell
cd "C:\Users\Kyung\Mud Game"
```

```powershell
git checkout main
```

```powershell
git tag -a v1.0.0 -m "Release v1.0.0 (S1 only)"
```

```powershell
git push origin v1.0.0
```

**✅ 완료!** 이제 GitHub Actions가 자동으로 실행될 거예요!

---

### 방법 2: RC 브랜치에 변경사항 추가하기 (선택사항)

**만약 나중에 RC 브랜치를 사용하고 싶다면:**

#### 1단계: RC 브랜치로 이동

```powershell
cd "C:\Users\Kyung\Mud Game"
git checkout chore/release-candidate-v1
```

#### 2단계: 작은 변경사항 추가 (예: README 수정)

```powershell
# README 파일에 한 줄 추가
echo "`n## Release Candidate v1`n" >> README.md
```

#### 3단계: 커밋하고 Push

```powershell
git add README.md
git commit -m "chore: add release candidate note"
git push origin chore/release-candidate-v1
```

**이제 PR을 만들 수 있어요!**

---

## 🎯 추천: 방법 1 사용하기

**가장 간단하고 빠른 방법:**

1. PR은 건너뛰기 (이미 main에 모든 게 있으니까요!)
2. 바로 태그 만들기
3. GitHub Actions 확인하기

**이렇게 하면 됩니다:**

```powershell
cd "C:\Users\Kyung\Mud Game"
git checkout main
git tag -a v1.0.0 -m "Release v1.0.0 (S1 only)"
git push origin v1.0.0
```

**그리고 GitHub에서:**
1. 창고 → **"Actions"** 탭 클릭
2. 워크플로우 실행 확인하기

---

## 💡 왜 이렇게 되었나요?

**정상적인 상황이에요!**

처음에 모든 코드를 한 번에 올렸기 때문에:
- `main` 브랜치 = 모든 코드 ✅
- `chore/release-candidate-v1` 브랜치 = 모든 코드 ✅
- 차이 = 없음! ✅

**결론:** 그냥 `main` 브랜치를 사용하면 됩니다!

---

## 🚀 다음 단계

### 1. 태그 만들기 (위의 방법 1 참고)

### 2. GitHub Actions 확인하기

1. GitHub 창고 → **"Actions"** 탭
2. **"Release Server Image"** 확인
3. **"Release Android Build"** 확인

**⏰ 5-10분 걸릴 수 있어요!**

---

## ❓ 질문

**Q: PR을 안 만들어도 되나요?**  
A: 네! 이미 모든 코드가 main에 있으니 괜찮아요!

**Q: RC 브랜치는 뭔가요?**  
A: 나중에 변경사항을 테스트할 때 사용하는 브랜치예요. 지금은 필요 없어요!

**Q: 그냥 main에서 태그 만들면 되나요?**  
A: 네! 그게 가장 간단해요! 😊

---

**작성일:** 2025-12-20  
**난이도:** ⭐ (초보자용)

