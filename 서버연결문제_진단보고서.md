# 서버 연결 문제 진단 보고서

## 🔴 발견된 문제

### 1. **치명적 문제: 서버가 실행되지 않음**
- ❌ `localhost:3000`에 연결할 수 없음
- ❌ 포트 3000이 리스닝되지 않음
- **원인**: 서버 프로세스가 실행되지 않음

### 2. 네트워크 설정
- ✅ PC IP 주소: `192.168.0.15` (또는 `192.168.0.10`)
- ✅ 앱의 NetworkDetector가 올바른 IP를 시도하도록 설정됨

## ✅ 해결 방법

### 방법 1: run-dev-android.ps1 스크립트 사용 (권장)
```powershell
.\run-dev-android.ps1
```
이 스크립트는 다음을 자동으로 수행합니다:
1. Docker 컨테이너 시작 (PostgreSQL, Redis)
2. 데이터베이스 마이그레이션
3. 시드 데이터 생성
4. 서버 시작 (새 PowerShell 창)
5. Flutter 앱 실행

### 방법 2: 수동으로 서버 시작
```powershell
# 1. 인프라 시작
pnpm infra:up

# 2. DB 대기
pnpm db:wait

# 3. 마이그레이션 (apps/server 디렉토리에서)
cd apps/server
npx prisma migrate deploy
npx prisma db seed

# 4. 서버 시작 (루트 디렉토리에서)
cd ../..
pnpm dev
```

## 📋 확인 체크리스트

서버가 시작된 후 다음을 확인하세요:

1. **서버 실행 확인**
   ```powershell
   # localhost 확인
   Invoke-WebRequest -Uri "http://localhost:3000/health" -UseBasicParsing
   
   # PC IP 확인 (방화벽 문제 확인)
   Invoke-WebRequest -Uri "http://192.168.0.15:3000/health" -UseBasicParsing
   ```

2. **포트 리스닝 확인**
   ```powershell
   netstat -ano | findstr ":3000" | findstr "LISTENING"
   ```

3. **방화벽 설정** (PC IP로 접근이 안 될 경우)
   ```powershell
   # 관리자 권한으로 실행
   netsh advfirewall firewall add rule name="MUD Game Server Port 3000" dir=in action=allow protocol=TCP localport=3000
   ```

4. **앱 설정 확인**
   - 설정 화면에서 서버 URL 확인
   - REST URL: `http://192.168.0.15:3000`
   - WS URL: `ws://192.168.0.15:3000`

## 🔍 추가 디버깅

앱에서 여전히 연결이 안 될 경우:

1. **로그 확인**
   ```powershell
   & "C:\Users\Kyung\AppData\Local\Android\Sdk\platform-tools\adb.exe" -s R3CT80GM39T logcat | Select-String -Pattern "ApiClient|SessionState|NetworkDetector"
   ```

2. **네트워크 확인**
   - PC와 Android 기기가 같은 Wi-Fi에 연결되어 있는지 확인
   - 기기의 Wi-Fi IP 확인 (앱의 NetworkDetector 로그에서)

## 📝 현재 설정 상태

- ✅ AndroidManifest.xml에 INTERNET 권한 추가됨
- ✅ NetworkDetector가 192.168.0.15를 우선순위로 시도
- ✅ SessionState.init()에서 자동 IP 감지 구현됨
- ✅ ApiClient에 상세한 로그 추가됨

**다음 단계: 서버를 시작하세요!**

